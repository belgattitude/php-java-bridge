/*
Gradle init script to add PDFBox dependencies to the build
*/

initscript {
    repositories {
        mavenCentral()
    }
    dependencies {
    }
}

allprojects {
    apply plugin: 'java'
    dependencies {
        runtime 'org.apache.pdfbox:pdfbox:2.+'
    }
}
