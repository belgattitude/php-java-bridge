/*
Gradle init script to add iText dependencies to the build
*/

initscript {
    repositories {
        mavenCentral()
    }
    dependencies {
    }
}

allprojects {
    apply plugin: 'java'
    dependencies {
        runtime 'com.google.guava:guava:21+'
    }
}
